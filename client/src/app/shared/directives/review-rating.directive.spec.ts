import { ReviewRatingDirective } from './review-rating.directive';

describe('ReviewRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new ReviewRatingDirective();
    expect(directive).toBeTruthy();
  });
});
