import { ReviewRating.Directive.TsDirective } from './review-rating.directive.ts.directive';

describe('ReviewRating.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new ReviewRating.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
