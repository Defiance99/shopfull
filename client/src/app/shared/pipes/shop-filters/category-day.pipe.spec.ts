import { CategoryDayPipe } from './category-day.pipe';

describe('CategoryDayPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryDayPipe();
    expect(pipe).toBeTruthy();
  });
});
